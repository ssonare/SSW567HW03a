version: 2.1
jobs:
  build:
    docker:
      - image: cimg/python:3.9
    steps:
      - checkout
      - run: pip install -r requirements.txt
      - run: python -m unittest discover -s GitHubApi567-hw4a

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
